#include <bits/stdc++.h>  
int main() {  
    double a, b, c;  
    std::cin >> a >> b >> c;  
    std::cout << "Trapezoid area:";  
    std::cout << std::fixed << std::setprecision(1) << (a + b) * c / 2 << "\n";  
    return 0;  
} 